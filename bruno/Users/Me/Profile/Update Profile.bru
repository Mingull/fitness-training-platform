meta {
  name: Update Profile
  type: http
  seq: 2
}

patch {
  url: {{host}}/users/me/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "firstName": "Tester",
    "lastName": "Account"
  }
}
