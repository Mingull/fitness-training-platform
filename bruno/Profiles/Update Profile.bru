meta {
  name: Update Profile
  type: http
  seq: 2
}

patch {
  url: {{host}}/profiles/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "firstName": "Tester",
    "lastName": "Account"
  }
}
