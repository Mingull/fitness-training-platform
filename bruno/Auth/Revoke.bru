meta {
  name: Revoke
  type: http
  seq: 5
}

post {
  url: {{host}}/auth/revoke
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refreshToken": "{{refresh_token}}"
  }
}
